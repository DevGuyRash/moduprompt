{
  "name": "moduprompt",
  "private": true,
  "packageManager": "pnpm@9.7.0",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "schema": "pnpm -r schema"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
