export const buildArtifact = () => {
  throw new Error('Server-side export not available in harness environment.');
};

export const renderHtml = () => {
  throw new Error('Server-side export not available in harness environment.');
};
